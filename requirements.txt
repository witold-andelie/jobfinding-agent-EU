# Runtime deps for Streamlit Community Cloud (the deployed UI). Scrapling is
# intentionally excluded — Live mode doesn't crawl career pages, and Cloud can't run
# the Playwright browsers anyway.
streamlit==1.61.1  # pin: >=1.62 introduces a /api/v2/user/details frontend call that Streamlit Community Cloud's gateway returns 404 for, blanking the page on any sidebar interaction
pydantic>=2.7
pydantic-settings>=2.3
certifi>=2024.0
openai==2.53.0       # DeepSeek (chat) + Jina (embeddings), both OpenAI-protocol; pin to the version tested locally
supabase>=2.0        # persistence
python-docx>=1.1     # CV variant export
